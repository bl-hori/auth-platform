# Auth Platform Backend Configuration
# Auth Platform BackendのベースURL（デフォルト: http://localhost:8080）
NEXT_PUBLIC_AUTH_BACKEND_URL=http://localhost:8080

# Auth Platform APIキー（実際の環境では適切なキーを設定）
NEXT_PUBLIC_AUTH_API_KEY=demo-api-key

# 組織ID（Auth Platformで作成した組織のID）
NEXT_PUBLIC_AUTH_ORGANIZATION_ID=demo-org-id

# Application Configuration
NEXT_PUBLIC_APP_NAME="Vendor Registration Example"

# Debug Mode（開発時のログ出力制御）
# NODE_ENV=development の場合、自動的にデバッグモードが有効になります
